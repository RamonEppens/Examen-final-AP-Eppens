# Raw data (too large / generated)
data/raw/

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*.pyo

# Jupyter checkpoints
.ipynb_checkpoints/

# Virtual environments
.venv/
venv/
env/

# OS
.DS_Store
Thumbs.db

# Model artifacts (large binaries — commit selectively)
models/*.pkl
models/*.joblib
models/*.h5

# Secrets / credentials
*.env
.env

# Optuna study DB / raw run logs (regenerable, resumable locally — not versioned)
tuning/*.db
tuning/run_log.txt
